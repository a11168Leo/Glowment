// Salões e barbearias cadastrados.
// Enquanto o backend não existe, não há nenhum: as seções ficam escondidas.
// Quando a API estiver pronta, é só buscar os dados dela aqui.

export async function buscarSaloes() {
  return []
}

export async function buscarBarbearias() {
  return []
}
